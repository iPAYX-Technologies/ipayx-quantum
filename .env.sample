# ========================================
# DRY_RUN Mode (Atlas UI Blueprint)
# ========================================
# Set to 'false' to enable real API calls (not recommended for CI/testing)
# When 'true', all API calls return mock data with no external network calls
VITE_DRY_RUN=true

# ========================================
# Note: No secrets or API keys required for DRY_RUN mode
# ========================================
# The Atlas UI Blueprint operates entirely in DRY_RUN mode by default
# No external integrations, no real network calls in CI
# All functionality uses local stubs and mock data
